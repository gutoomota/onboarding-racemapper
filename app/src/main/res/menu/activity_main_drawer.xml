<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_run"
                android:icon="@drawable/ic_run"
                android:title="@string/title_run"/>
        <item
                android:id="@+id/nav_run_history"
                android:icon="@drawable/ic_history"
                android:title="@string/title_run_history" />
    </group>

    <item android:title="@string/menu_weather">
        <menu>
            <item
                    android:id="@+id/nav_weather"
                    android:icon="@drawable/ic_weather"
                    android:title="@string/menu_submenu_weather" />
        </menu>
    </item>

</menu>
